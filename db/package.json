{
  "name": "fems-db",
  "version": "1.0.0",
  "private": true,
  "description": "Database migrations, seed data and backup tooling for the Fire Extinguisher Management System",
  "type": "commonjs",
  "scripts": {
    "migrate": "node migrate.js",
    "seed": "node seed.js",
    "reset": "node migrate.js && node seed.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  }
}
